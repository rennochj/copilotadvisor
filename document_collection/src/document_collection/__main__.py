"""Main entry point for document collection when run as a module.

This allows the package to be run with:
python -m document_collection
"""

from document_collection.cli.main import main

if __name__ == "__main__":
    main()
